# Toggle Mongo usage (0 = in-memory JSON, 1 = MongoDB)
USE_MONGO=0

# Mongo connection string (inside docker-compose, the hostname 'mongo' works).
# If running the API directly on your host (not in the compose network), change 'mongo' -> 'localhost'.
# The code now auto-falls back to localhost if 'mongo' DNS fails, but setting it explicitly avoids a retry.
# Example (host run): MONGODB_URI=mongodb://bbr:bbrpass@localhost:27017/?authSource=admin
# NOTE: The mongodb/mongodb-atlas-local image runs a replica-set style topology; if you encounter
#       NotPrimaryError issues you can instead switch to the simpler 'mongo:7' image in docker-compose.
MONGODB_URI=mongodb://bbr:bbrpass@mongo:27017/?authSource=admin
MONGO_HOST_PORT=27017

# Root credentials for docker-compose Mongo service (used only at container init)
MONGODB_ROOT_USER=bbr
MONGODB_ROOT_PASS=bbrpass

# Ingestion caps (optional overrides)
BBR_USERS_CAP=50000
BBR_SLEEP_CAP=500000
BBR_ACTIVITY_CAP=1000000
BBR_MEASUREMENTS_CAP=100000
